export enum UserRole {
    ADMIN = "admin",
    PROFESSIONAL = "professional",
    PATIENT = "patient",
}

export enum Gender {
    MALE,
    FEMALE,
    NON_BINARY,
    OTHER,
}

export enum ContactType {
    PHONE,
    EMAIL,
}